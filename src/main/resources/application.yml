spring:
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/clinica
    username: postgres
    password: caio1106
    driverClassName: org.postgresql.Driver